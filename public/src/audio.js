export class AudioEngine{
 constructor(){this.enabled=false}
 init(){if(this.ctx)return;this.ctx=new(window.AudioContext||window.webkitAudioContext)();this.master=this.ctx.createGain();this.master.gain.value=.18;this.master.connect(this.ctx.destination);this.engine=this.ctx.createOscillator();this.engine.type='sawtooth';this.engine.frequency.value=38;const low=this.ctx.createBiquadFilter();low.type='lowpass';low.frequency.value=150;this.gain=this.ctx.createGain();this.gain.gain.value=.08;this.engine.connect(low).connect(this.gain).connect(this.master);this.engine.start();this.hydraulic=this.ctx.createOscillator();this.hydraulic.type='sine';this.hydraulic.frequency.value=190;this.hgain=this.ctx.createGain();this.hgain.gain.value=0;this.hydraulic.connect(this.hgain).connect(this.master);this.hydraulic.start()}
 toggle(){this.init();this.enabled=!this.enabled;this.ctx.resume();this.master.gain.setTargetAtTime(this.enabled?.22:0,this.ctx.currentTime,.05);return this.enabled}
 update(speed,arm){if(!this.ctx)return;this.engine.frequency.setTargetAtTime(34+Math.abs(speed)*9,this.ctx.currentTime,.15);this.gain.gain.setTargetAtTime(.1+Math.abs(speed)*.025,this.ctx.currentTime,.1);this.hgain.gain.setTargetAtTime(arm?.025:0,this.ctx.currentTime,.1)}
 sound(type){if(!this.enabled||!this.ctx)return;const now=this.ctx.currentTime;const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type==='success'?'sine':'triangle';o.frequency.setValueAtTime(type==='success'?660:type==='attach'?350:130,now);o.frequency.exponentialRampToValueAtTime(type==='success'?990:50,now+.23);g.gain.setValueAtTime(.5,now);g.gain.exponentialRampToValueAtTime(.001,now+.5);o.connect(g).connect(this.master);o.start();o.stop(now+.5)}
}
